@page
@model SellAnItemModel

@{
    ViewData["Title"] = "Sell An Item";
}

@using (Html.BeginForm("Item_DE", "SellAnItem", FormMethod.Post, new { id = "ITEM_DE" }))
{
<body>
    <form method = "post">
        <legend>Sell an Item</legend>
        <p><label for="item">Item:</label>
            @Html.TextBoxFor(a => a.itemName, new { @class = "form-control" })
        </p>

        <p><label for="price">Price:</label>
            @Html.TextBoxFor(a => a.price, new { @class = "form-control" })
        </p>

        <p><label for="date">Date:</label>
            @Html.TextBoxFor(a => a.date, new { @class = "form-control" })
        </p>

        <p><input type="submit" name="buttonSubmit" value="Complete Transaction" /></p>
    </form>
</body>
}
